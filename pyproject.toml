[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cau_st"
version = "0.1.0"
description = "this is a package of inference"
readme = "README.md"
authors = [
    {name = "你的名字", email = "your@email.com"}
]
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = [
    "requests>=2.28.0",
    "numpy>=1.21.0"
]

[project.urls]
Homepage = "https://github.com/你的用户名/你的仓库"