# Runtime dependencies for CauST (core, dependency-light backbone).
# For development tooling, install: pip install -e ".[dev]"
numpy>=1.21.0
scipy>=1.7.0
scikit-learn>=1.0
anndata>=0.8

# Optional: real GNN backbones (STAGATE / GraphST) and full ST workflows.
# Install with: pip install -e ".[stagate]"
# torch>=1.10
# scanpy>=1.9
